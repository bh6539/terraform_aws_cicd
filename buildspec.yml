version: 0.2

phases:
  build:
    commands:
      - aws ecr-public get-login-password --region us-east-1 | docker login --username AWS --password-stdin public.ecr.aws/q5s4z6w3
      - docker build -t my-image-registry .
      - docker tag my-image-registry:latest public.ecr.aws/q5s4z6w3/my-image-registry:latest
      - docker push public.ecr.aws/q5s4z6w3/my-image-registry:latest
